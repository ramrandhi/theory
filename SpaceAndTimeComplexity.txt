TIME COMPLEXITY: 
-> it is the relation between time grows as the input grows.
-> o(1) < o(log n) < o(n) < o(2n).

BIG OMEGA : Opposite to big o.
-> it starts with lower bound of omega(n3) which is worst case complexity for big o notation which is upper bound.

LITTLE O NOTATION : THIS IS ALSO GIVES THE UPPER BOUND.

SPACE COMPLEXITY : AUXILIARY SPACE + TEMPORARY SPACE USED BY AN ALGORITHM.
